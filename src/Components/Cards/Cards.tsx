import React from "react"
import {ThemeContext} from '../../Pages/MainPage'

export default function Card(){

    let contextData = React.useContext(ThemeContext)


    return(
        <div className="card">

        </div>
       
    )
}